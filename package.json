{
  "name": "@prairie/push-relay-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit && tsc -p test/tsconfig.json --noEmit",
    "lint": "oxlint src test",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "node ./scripts/coverage-unsupported.mjs"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.18.4",
    "@cloudflare/workers-types": "5.20260710.1",
    "typescript": "7.0.2",
    "vitest": "4.1.10",
    "wrangler": "4.110.0",
    "oxlint": "1.75.0",
    "oxlint-tsgolint": "7.0.2001"
  }
}
